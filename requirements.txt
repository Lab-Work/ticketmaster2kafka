# LV connectors — installed in the Dockerfile's builder stage from the private
# Lab-Work repos (see Dockerfile). Lower bounds only, so a connector fix
# reaches this service on its next image build. During local development,
# point pip at the sibling checkouts:
#
#   pip install -e ../lv_telemetry_connector
#   pip install -e ../lv_kafka_connector
#   pip install -e ../lv_db_connector

./lv_telemetry_connector
./lv_kafka_connector
./lv_db_connector

# Third-party. confluent-kafka and psycopg come transitively via the
# connectors; don't list them here.
requests==2.31.0
python-dotenv
pytz
tzdata
filelock
numpy
